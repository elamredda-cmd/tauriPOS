{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "customer-display",
  "description": "Read-only capability for the customer display window",
  "windows": ["customer-display"],
  "permissions": [
    "core:default",
    "sql:allow-load",
    "sql:allow-select",
    "sql:allow-close"
  ]
}
